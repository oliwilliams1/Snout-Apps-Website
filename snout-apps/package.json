{
  "name": "snout-apps",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --watch",
    "build:css": "tailwindcss -i ./src/styles.css -o ./src/output.css --watch",
    "tauri:dev": "tauri dev",
    "dev": "concurrently \"npm run build\" \"npm run tauri:dev\" \"npm run build:css\""
    },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "autoprefixer": "^10.4.20",
    "concurrently": "^7.6.0",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.3",
    "vite": "^5.3.1"
  }
}
